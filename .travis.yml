nguage: go

go:
  - 1.13.x
  - tip

before_install: cd cmd/omega

script:
  - go test ./...
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)

env:
  - GO111MODULE=on
